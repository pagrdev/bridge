{
  "name": "pagr-bridge",
  "version": "0.2.0",
  "private": true,
  "description": "Pagr bridge — open-source macOS daemon that lets Pagr's cloud drive your local Claude Code and Codex sessions through typed, signed commands. No remote shell.",
  "license": "Apache-2.0",
  "author": "Stagberry Labs LLC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pagrdev/bridge.git"
  },
  "homepage": "https://github.com/pagrdev/bridge#readme",
  "bugs": {
    "url": "https://github.com/pagrdev/bridge/issues"
  },
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "vitest run",
    "gate": "pnpm lint && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/node": "^22.10.0",
    "turbo": "^2.10.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0",
    "@vitest/coverage-v8": "^4.1.0"
  },
  "pnpm": {
    "overrides": {
      "vite": "^7.3.0"
    }
  }
}
